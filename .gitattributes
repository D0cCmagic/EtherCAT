# 禁止 Git 做行尾自动转换。
#
# 原因：本仓库的 CHECKSUMS.txt 是对"存入 Git 的字节"计算的哈希。
# 若允许 Git 在检出时做 CRLF 转换（Windows 默认行为），
# 同一文件在不同平台上算出的 SHA256 会不同，校验清单就失去意义。
#
# 设成 -text 后 Git 视为二进制、不做任何转换：
#   存入什么字节，检出就是什么字节 -> 哈希跨平台一致。
* -text
